<?php

namespace Database\Seeders;

use App\Models\Employee;
use App\Models\WikiPage;
use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\Schema;

class WikiKnowledgeBaseSeeder extends Seeder
{
    public function run(): void
    {
        if (! Schema::hasTable('wiki_pages')) {
            $this->command?->warn('Таблица wiki_pages отсутствует. Выполните миграции.');

            return;
        }

        $authorId = Employee::query()->orderBy('id')->value('id');
        if (! $authorId) {
            $this->command?->warn('Нет пользователей в employees — статьи wiki не созданы.');

            return;
        }

        $root = WikiPage::query()->updateOrCreate(
            ['slug' => 'it-master-obzor'],
            [
                'title' => 'IT-Master — обзор системы',
                'parent_id' => null,
                'sort_order' => 0,
                'created_by' => $authorId,
                'updated_by' => $authorId,
                'body' => <<<'MD'
# IT-Master

Внутренняя веб-система на **Laravel**: учёт пользователей и групп, расписание, заявки, инвентарь, портфолио, тестирование, настройки и почта.

Доступ к разделам задаётся **ролями**: у каждой роли набор прав (страниц). Если пункта нет в меню — у вашей роли нет соответствующего права.

Ниже — дочерние статьи по модулям. Их можно дополнять и править (при наличии права на редактирование базы знаний).

---

**Технологии:** PHP 8.2+, Laravel 12, Bootstrap 5, сессионная авторизация, мобильное API под префиксом `/api/mobile/`.
MD
            ]
        );

        $pages = [
            [
                'slug' => 'vhod-parol-profil',
                'sort_order' => 10,
                'title' => 'Вход, пароль и профиль',
                'body' => <<<'MD'
# Вход, пароль и профиль

## Вход
- Страница входа: корень сайта `/`.
- Логин и пароль выдаёт администратор. При многократных ошибках входа действует **ограничение частоты запросов** (защита от подбора пароля).

## Восстановление пароля
- Ссылка **«Восстановить пароль»** на форме входа.
- Нужен **настроенный SMTP** в системе и e-mail пользователя в карточке сотрудника.
- Письмо со ссылкой действует ограниченное время; при вводе несуществующего адреса показывается **нейтральное сообщение** (без раскрытия, есть ли такой пользователь).

## Профиль
- **Профиль** (`/profile`): просмотр данных учётной записи.
- Смена пароля: текущий пароль + новый (с подтверждением).
- Настройка **e-mail уведомлений** (если предусмотрено в форме профиля).

Право в системе: `dashboard` (главная и профиль).
MD
            ],
            [
                'slug' => 'glavnaya-zadachi-uvedomleniya',
                'sort_order' => 20,
                'title' => 'Главная, задачи и уведомления',
                'body' => <<<'MD'
# Главная панель, задачи и уведомления

## Дашборд (`/dashboard`)
- Отображается после входа (при наличии права).
- Блоки зависят от прав: **задачи**, **расписание студента**, **расписание преподавателя** (если выданы соответствующие права на расписание).

## Задачи
- Создание, просмотр, удаление — через формы с **POST/DELETE** (не через «простые ссылки»).
- Право: `tasks`.

## Уведомления
- Блок на главной; кнопка **«Прочитать всё»** помечает уведомления прочитанными.
- Право: `notifications` (для отметки прочитанным через отдельный маршрут).

Иконка колокольчика в шапке ведёт к якорю блока уведомлений на дашборде.
MD
            ],
            [
                'slug' => 'modul-zayavki',
                'sort_order' => 30,
                'title' => 'Модуль заявок',
                'body' => <<<'MD'
# Заявки

## Мои заявки
- Раздел **«Мои заявки»**: создание заявок, просмотр своих записей.
- Статусы заявок могут меняться **безопасными методами** (не через GET).
- Право: `orders_my`.

## Администрирование заявок
- Список заявок, смена статусов, работа с категориями.
- Право: `orders_admin`.

## Категории
- Управление категориями заявок (цвета, названия) — в том же разделе администрирования.

Файлы к заявкам сохраняются в хранилище приложения (публичный диск); при загрузке соблюдайте политику организации по типам файлов.
MD
            ],
            [
                'slug' => 'modul-inventar',
                'sort_order' => 40,
                'title' => 'Инвентарь',
                'body' => <<<'MD'
# Инвентарь

## Мой инвентарь
- Просмотр закреплённых за сотрудником единиц.
- Право: `inventory_my`.

## Управление
- Назначение, снятие, переназначение, справочники подразделений, типы, экспорт/печать — по отдельным правам администрирования инвентаря.
- Право: `inventory_admin`.

Операции изменения данных выполняются через формы (**POST** и т.д.), а не через «опасные» GET-ссылки.
MD
            ],
            [
                'slug' => 'polzovateli-roli-gruppy',
                'sort_order' => 50,
                'title' => 'Пользователи, роли и группы',
                'body' => <<<'MD'
# Пользователи, роли и группы

## Пользователи (сотрудники и студенты)
- Список, создание, редактирование, **активация/деактивация**, удаление — только у ролей с правом `employees_manage`.
- Новому пользователю может отправляться письмо с активацией (если настроена почта).

## Роли
- Настройка ролей и **набора прав на страницы** (`PageAccess`): какие разделы меню и URL доступны роли.
- Право: `roles_manage`.

## Группы
- Учебные группы, состав студентов, печать списков, открепление студента от группы.
- Право: `groups_manage`.

Доступ к настройкам системы и к БД — отдельные права (`settings`, `settings_database`), не смешивайте их с обычным управлением пользователями.
MD
            ],
            [
                'slug' => 'obrazovanie-fakultety-kafedry',
                'sort_order' => 60,
                'title' => 'Факультеты и кафедры',
                'body' => <<<'MD'
# Образовательная структура

## Факультеты
- CRUD факультетов.
- Право: `faculties_manage`.

## Кафедры
- Кафедры привязаны к факультетам.
- Право: `chairs_manage`.

Раздел **«Образование»** в меню виден, если выдано хотя бы одно из этих прав.
MD
            ],
            [
                'slug' => 'modul-raspisanie',
                'sort_order' => 70,
                'title' => 'Расписание',
                'body' => <<<'MD'
# Расписание

## Просмотр для студента
- Недельное расписание группы (при назначенной группе у пользователя).
- Право: `schedule_my`.
- Маршрут: `/schedule` (именованный `schedule.my`).

## Просмотр для преподавателя
- Занятия, где пользователь указан как преподаватель.
- Право: `schedule_teacher`.

## Конструктор расписания
- Редактирование записей расписания, копирование недели, пересчёт.
- Право: `schedule_constructor`.

## Настройки конструктора
- Предметы (справочник), параметры конструктора.
- Право: `schedule_constructor_settings`.

Удаление занятий и предметов — через **DELETE**-запросы из форм, с CSRF-токеном.
MD
            ],
            [
                'slug' => 'modul-portfolio',
                'sort_order' => 80,
                'title' => 'Портфолио',
                'body' => <<<'MD'
# Портфолио

## Мои материалы
- Загрузка файлов, привязка к типам записей.
- Право: `portfolio`.
- Файлы отдаются через контроллер с проверкой **владельца** или модератора.

## Типы и роли портфолио
- Справочники для классификации работ.
- Право: `portfolio_types`.

## Подтверждение
- Модерация заявок на публикацию (одобрение/отклонение).
- Право: `portfolio_confirm`.

Видимость списка портфолио для обычного пользователя ограничена **своими** записями; расширенный доступ у ролей с подтверждением или настройками.
MD
            ],
            [
                'slug' => 'modul-testirovanie',
                'sort_order' => 90,
                'title' => 'Тестирование',
                'body' => <<<'MD'
# Тестирование

## Студент
- Список доступных тестов группы, прохождение, просмотр результатов/разбора (в рамках прав).
- Право: `student_tests`.

## Администрирование тестов
- Создание тестов, вопросы, назначение группам, включение/выключение.
- Право: `tests_admin`.

## Статистика
- Сводки и выгрузки по попыткам (для ролей с доступом к статистике).
- Право: `tests_stats`.

## Мобильное приложение
- API: префикс `/api/mobile/` (логин с **ограничением частоты**, выдача токена, расписание, уведомления, тесты).

Тесты и расписание в приложении завязаны на те же права и группу пользователя, что и в веб-интерфейсе.
MD
            ],
            [
                'slug' => 'menedzher-paroley',
                'sort_order' => 100,
                'title' => 'Менеджер паролей',
                'body' => <<<'MD'
# Менеджер паролей

Персональное хранилище записей (название, URL, логин, пароль, заметки).

- Пароли хранятся **в зашифрованном виде** (шифрование Laravel).
- Просмотр пароля по кнопке — запрос к серверу; видеть может только **владелец** записи.
- Право: `password_manager`.

Не храните в менеджере пароли от критичных систем без политики организации на резервное копирование и доступ.
MD
            ],
            [
                'slug' => 'baza-znaniy-wiki',
                'sort_order' => 110,
                'title' => 'База знаний (wiki)',
                'body' => <<<'MD'
# База знаний

Внутренняя **wiki** с разметкой **Markdown** (заголовки, списки, код, таблицы — по возможностям парсера GFM).

## Права
- **Чтение** (`knowledge_wiki`): список статей и просмотр.
- **Редактирование** (`knowledge_wiki_edit`): создание, правка, удаление.

## Структура
- Статьи могут быть **вложенными** (родительский раздел в форме редактирования).
- Адрес страницы — **slug** (латиница и дефисы). Системные имена `create`, `store`, `edit` заняты маршрутами и для статей не используются.

## Советы
- Длинные инструкции разбивайте на подстраницы.
- После изменений проверяйте отображение на мобильном (адаптивная вёрстка раздела).
MD
            ],
            [
                'slug' => 'nastroyki-sistemy',
                'sort_order' => 120,
                'title' => 'Настройки системы и БД',
                'body' => <<<'MD'
# Настройки (для администраторов)

## Общие и почта
- Название сайта, режим «сайт отключён», аутентификация, **SMTP** (хост, порт, шифрование, отправитель).
- Тестовое письмо и логи отправки — в интерфейсе настроек почты.
- Для SMTP рекомендуется **строгая проверка TLS** (по конфигурации); ослабление только осознанно, через переменные окружения.

## Операции с Git (если включены)
- Проверка обновлений, pull, ветка деплоя — отдельные POST-маршруты; доступ ограничен правом `settings` и дополнительными проверками в контроллере.

## База данных
- Профили подключения, миграции, инициализация удалённой БД — право `settings_database`.
- Опасные операции требуют осознанного использования и резервных копий.

## Безопасность (кратко)
- Чувствительные маршруты `/settings` без явного маппинга в правах для подпутей **закрыты** (fail-closed для настроек).
- Сессии и cookie: в production задайте `SESSION_SECURE_COOKIE`, `SESSION_HTTP_ONLY`, при необходимости `SESSION_ENCRYPT` (см. `.env.example`).

Подробный план мер см. файл `docs/security-remediation-plan.md` в репозитории (если поставляется с проектом).
MD
            ],
        ];

        foreach ($pages as $row) {
            WikiPage::query()->updateOrCreate(
                ['slug' => $row['slug']],
                [
                    'title' => $row['title'],
                    'body' => $row['body'],
                    'parent_id' => $root->id,
                    'sort_order' => $row['sort_order'],
                    'created_by' => $authorId,
                    'updated_by' => $authorId,
                ]
            );
        }

        $this->command?->info('База знаний: создано/обновлено '.(1 + count($pages)).' статей (корень + дочерние).');
    }
}
